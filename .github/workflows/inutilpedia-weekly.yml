name: inutilpedia-weekly

on:
  schedule:
    - cron: "0 7 * * 6"
  workflow_dispatch:

jobs:
  call:
    uses: ./.github/workflows/inutilpedia-run.yml
    with:
      mode: weekly
    secrets: inherit
